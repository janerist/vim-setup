Install:

1. clone into ~/.vim
$ git clone http://github.com/janerist/vim-setup.git ~/.vim

2. symlink .vimrc
$ ln -s ~/.vim/.vimrc ~/.vimrc

3. get Vundle
$ git clone http://github.com/gmarik/vundle.git ~/.vim/bundle/vundle

4. install bundles
$ vim +BundleInstall! +q

6. setup bundles
 a) Command-T
    $ cd ~/.vim/bundle/Command-T && rake make (requires Ruby)
 b) pyflakes
    $ cd ~/.vim/bundle/pyflakes-vim && git submodule init && git submodule update
